# Flask Todo Web Application

This is a simple, full-stack Todo list application built using the Flask web framework. It allows users to add, view, and manage their daily tasks.

## 🛠 Technologies Used

* *Backend:* Python 3 and Flask
* *Database:* SQLite (The data is stored in the todo.db file)
* *Frontend:* HTML/CSS (Styled with Bootstrap for responsive design)

## ✨ Features

* Add new tasks (Todos).
* View the list of all existing tasks.
* Update/Edit existing task details.
* Delete tasks from the list.

## 🚀 How to Run Locally

Follow these steps to set up and run the application on your local machine:

### 1. Clone the Repository
```bash
git clone [YOUR REPOSITORY URL HERE]
cd flask
